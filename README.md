# soomin007.github.io

게임 허브. 인스타 프로필에 거는 링크 하나가 여기로 온다.

**라이브: https://soomin007.github.io/**

홈은 상점(피처드 캐러셀 + 캡슐 그리드), 게임을 누르면 **그 게임의 전용 소개 페이지**(`g/<id>/`)로
간다. 전용 페이지는 각 게임 본편의 팔레트·실제 데이터·실규칙 축소판 맛보기를 담은 단일 HTML 이고,
자기 OG 썸네일과 제목을 가져서 어디에 공유해도 그 게임답게 보인다.

## 구조

```
index.html        홈 뼈대. OG 태그, 폰트 로드, 홈/폴백 상세 컨테이너
g/<id>/           게임 전용 소개 페이지 (5개 게임 전부). 스타일·스크립트·맛보기 내장 단일
                  HTML + 전용 og.png. games.js 의 page 필드로 연결되고 옛 #g/<id> 는
                  리다이렉트된다
styles.css        홈 + 폴백 상세(.t-plain, page 필드 없는 새 게임용)
games.js          게임 데이터 배열. 카드·피처드·폴백 상세가 읽는다
app.js            games.js 를 읽어 홈을 그림 + 라우팅(전용 페이지 리다이렉트) + 기기/유입 적응
assets/           게임별 캡슐 이미지 (1232x706 webp)
assets/shots/     스크린샷 소재 (<게임id>-*.webp). 전용 페이지들이 참조
og.png            허브 공유 썸네일 (1200x630)
icon.png          파비콘 (256x256)
known-issues.md   반복 위험 함정 모음. 코드 작성 전 필독
DESIGN_HANDOFF.md 디자인 도구에 넘기는 브리프 (제약·데이터·에셋 URL)
```

빌드 과정이 없다. HTML/CSS/JS 정적 파일뿐이라 push 하면 바로 반영된다.

## 적응형 동작

- **반응형**: 375px~1920px. 폰에선 캐러셀 화살표가 숨고 스와이프·점으로 넘긴다.
- **기기**: 터치 기기에선 `mobile: true` 게임이 먼저 정렬된다. 키보드 필요 게임엔 "키보드 필요" 배지.
- **유입 경로**: `?from=insta` `?from=github` `?from=itch` 쿼리(또는 referrer 자동 감지)로
  상단 환영 배너가 바뀐다. 인스타 프로필에는 `https://soomin007.github.io/?from=insta` 를 걸면 된다.
- **라우팅**: 카드를 누르면 그 게임의 전용 페이지(`g/<id>/`)로. 옛 `#g/<id>` 링크는 자동 리다이렉트.
  page 필드가 없는 게임만 내장 폴백 상세(#g/<id>, 크로스페이드 전환)를 쓴다.
- **모션**: `prefers-reduced-motion` 이면 자동 회전·연출 애니메이션이 꺼진다.

## 게임 추가

1. 캡슐 이미지 하나 생성 (대표 스크린샷/키아트 → 16:9):
   ```
   ffmpeg -y -i <원본> -vf "crop=...,scale=1232:706" -c:v libwebp -q:v 82 assets/<게임id>.webp
   ```
2. `games.js` 의 `GAMES` 배열에 항목 추가 (필드 설명은 파일 상단 주석이 단일 진실).
   `page` 를 비워 두면 내장 폴백 상세가 나오므로 이 단계만으로도 배포 가능하다.
   - 문구는 표준 안내체(합쇼체)로. em dash(U+2014)는 쓰지 않는다.
   - `facts`·`shots` 캡션: **검증된 사실만**. 플레이 시간 등 수치를 지어내지 않는다.
3. (권장) 전용 소개 페이지 `g/<게임id>/index.html` 을 만든다. 기존 5개 페이지 중 톤이
   비슷한 것을 복사해 시작하되, **known-issues.md 를 먼저 읽고** 게임 본편의 팔레트·실제
   데이터로 채운다. 전용 `og.png`(1200x630)도 함께. 완성되면 games.js 에 `page` 를 연결한다.

배열 순서가 곧 데스크톱 화면 순서다. 처음 온 사람이 제일 쉽게 붙는 게임을 맨 위에 둔다.

## 남은 일

- [ ] (선택) 다른 게임을 itch.io에 올리게 되면 games.js 의 itch 칸 채우기.
      itch 업로드 자체는 의무가 아니다. 허브는 GitHub Pages 링크만으로 완결된다
- [ ] 커스텀 도메인을 살 경우: 이 저장소 Settings → Pages → Custom domain 에 등록.
      그러면 프로젝트 페이지도 전부 새 도메인 아래로 따라온다. 기존 주소는 깃허브가 리다이렉트한다
